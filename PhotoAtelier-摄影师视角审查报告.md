# PhotoAtelier 摄影工作流应用 — 专业摄影师深度审查报告

---

## 一、摄影专业术语准确性

### 1.1 术语使用总体评价：**中等偏上，存在若干不准确之处**

**准确使用的术语：**

- **光圈**：模板中光圈值使用正确，如 `f/1.4-2.8`、`f/2-5.6`（第 4343-4416 行）。光圈数值与场景匹配合理（人像大光圈、产品小光圈）。
- **快门速度**：`1/200s`、`1/1000s` 等数值正确（第 4344 行）。
- **ISO**：`ISO100-400`、`ISO800` 等数值范围合理（第 4344 行）。
- **焦段**：`35mm`、`50mm`、`85mm`、`135mm` 等焦段推荐与拍摄场景匹配正确（第 5208 行）。
- **色温**：`5500K`（日光白）、`4000K`（暖光）、`5000K` 等数值准确（第 4695、4710、4893 行）。
- **布光术语**：`双灯柔光箱（左右45度）`、`发灯勾勒轮廓`、`蜂巢罩控制光域`、`轮廓光分离人物与背景` 等布光描述专业准确（第 4740、4803 行）。
- **后期术语**：`色调分离`、`黑位/白位`、`S型曲线`、`HSL调整`、`清晰度`、`自然饱和度` 等 Lightroom 参数名称完全正确（第 6134-6144 行）。

**存在问题的术语：**

| 问题 | 位置 | 具体描述 | 严重程度 |
|------|------|----------|----------|
| ND滤镜描述不准确 | 第 4374 行 | `ND滤镜(f/8光圈阳光下)` — ND滤镜的作用是在大光圈下降低进光量以实现慢门或浅景深，而非"阳光下使用f/8"。正确理解应为：阳光下想用大光圈（如f/2）时需要ND滤镜来降低快门速度不超过闪光同步速度 | 中 |
| `leaning on desk` 拼写 | 第 4693 行 | 商务形象照模板中出现英文残留 `leaning on desk`，混入中文模板中 | 低 |
| "空气切割刀" | 第 4469 行 | `Sony 85mm f/1.4 GM II` 的备注写"空气切割刀"，这是网络俗称而非专业术语。专业场景中应标注"焦外过渡柔滑，焦内锐利" | 低 |
| "人像王" | 第 4469 行 | `Sony 85mm f/1.4 GM II` 备注为"人像王"，同上，过于口语化 | 低 |
| "平民人像王" | 第 4469 行 | `Sigma 85mm f/1.4 DG DN Art` 备注为"平民人像王" | 低 |
| 缺少测光模式 | 全局 | 整个应用没有任何关于测光模式（评价测光/点测光/中央重点）的建议 | 中 |
| 缺少对焦模式 | 全局 | 没有关于对焦模式（单次AF/连续AF/眼部追焦）的建议 | 中 |
| 缺少文件格式建议 | 全局 | 虽然第 4445 行提到"RAW格式拍摄"，但模板中普遍缺少 RAW/JPEG 选择、色彩空间（sRGB/AdobeRGB）等专业设置建议 | 中 |

### 1.2 改进建议

1. **修正 ND 滤镜描述**（第 4374 行）：改为 `ND滤镜（阳光下大光圈拍摄必备，降低进光量避免过曝）`
2. **清理口语化备注**：将设备备注改为专业描述
3. **增加缺失的拍摄参数**：在模板和 AI 提示词中加入测光模式、对焦模式、文件格式、色彩空间建议
4. **修正英文残留**（第 4693 行）：`leaning on desk` 改为 `倚靠办公桌`

---

## 二、AI 提示词质量分析

### 2.1 `buildImagePrompt` / `buildAdvancedImagePrompt` 函数分析

**位置**：第 5766-5866 行

**当前提示词结构**：
```
professional photography, {风格翻译}, {主题}, {模特特征翻译}, {场景}, {光线}, {后期色调}, cinematic composition, high detail, 8K, masterpiece
```

**严重问题 — 缺少核心摄影参数**：

| 缺失参数 | 重要性 | 说明 |
|----------|--------|------|
| **镜头焦段** | 极高 | 如 `shot on 85mm f/1.4`，直接影响画面透视和景深效果 |
| **光圈值** | 极高 | 如 `f/1.4 shallow depth of field`，决定虚化程度 |
| **具体布光方案** | 高 | 如 `Rembrandt lighting`、`butterfly lighting`、`split lighting` |
| **机身/画幅** | 中 | 如 `full frame sensor`、`medium format` |
| **胶片类型/年代** | 中 | 复古风格应指定 `Kodak Portra 400`、`Fuji Superia` 等 |
| **构图方式** | 中 | 如 `rule of thirds`、`center composition`、`negative space` |

**当前提示词示例**（第 5857-5863 行）：
```javascript
let prompt = `professional photography, ${stylePrompt}`;
if (themePrompt) prompt += `, ${themePrompt}`;
if (modelParts.length) prompt += `, ${modelParts.join(', ')}`;
if (sceneDetail) prompt += `, ${sceneDetail}`;
if (lightDetail) prompt += `, ${lightDetail}`;
if (colorDetail) prompt += `, ${colorDetail}`;
prompt += `, cinematic composition, high detail, 8K, masterpiece`;
```

**问题**：`cinematic composition` 过于笼统，`8K masterpiece` 是 AI 绘图通用后缀而非摄影专业描述。

### 2.2 `buildImagePromptVariant` 函数分析

**位置**：第 5869-5883 行

```javascript
const variantMap = {
    '正面半身': 'front view, half-body portrait, facing camera, natural standing',
    '侧面全身': 'side view, full body, looking back over shoulder',
    // ...
};
return `${basePrompt}, ${variantMap[variant.angle] || variant.desc}, ${variant.desc}`;
```

**问题**：
- 变体描述过于简单，缺少与摄影参数的结合
- 没有根据不同角度调整焦段建议（如全身应用 35mm，半身应用 85mm）
- 缺少构图指导（三分法、居中、对角线等）

### 2.3 `getEnglishStyle` 函数分析

**位置**：第 5977-5993 行

风格翻译映射基本准确，但缺少一些常见风格：
- 缺少 `街拍`、`运动`、`夜景`、`机车`、`樱花`、`居家`、`甜品` 等模板中已存在但映射中缺失的风格
- `时尚` 和 `杂志` 的翻译有重叠

### 2.4 改进建议

```javascript
// 建议的增强版提示词结构
function buildAdvancedImagePrompt(plan) {
    // ... 现有逻辑 ...

    // 新增：根据风格推荐镜头
    const lensRecommendation = getLensForStyle(inp.style);

    // 新增：根据风格推荐布光
    const lightingSetup = getLightingForStyle(inp.style);

    // 新增：构图方式
    const composition = getCompositionForStyle(inp.style);

    prompt += `, shot on ${lensRecommendation}`;
    prompt += `, ${lensRecommendation.includes('f/1.4') ? 'shallow depth of field, beautiful bokeh' : 'deep depth of field'}`;
    prompt += `, ${lightingSetup}`;
    prompt += `, ${composition}`;
}
```

---

## 三、数据结构专业性审查

### 3.1 器材（Equipment）数据结构

**位置**：第 4465-4480 行（示例数据）、第 4527 行（渲染函数）、第 4271 行（分类定义）

**当前结构**：
```javascript
{ id, n(名称), c(分类), note(备注), at(添加时间) }
```

**分类定义**（第 4271 行）：
```javascript
const CATS = {
    camera: {n:'相机机身', ic:'...'},
    lens: {n:'镜头', ic:'...'},
    light: {n:'灯光设备', ic:'...'},
    audio: {n:'音频', ic:'...'},
    tripod: {n:'三脚架/稳定器', ic:'...'},
    filter: {n:'滤镜/附件', ic:'...'},
    other: {n:'其他', ic:'...'}
};
```

**严重缺失的字段**：

| 缺失字段 | 重要性 | 说明 |
|----------|--------|------|
| **品牌/型号** | 极高 | 当前 `n` 字段混合了名称和型号（如 `Sony A7M4`），但缺乏结构化字段 |
| **序列号** | 高 | 设备管理和保修追踪 |
| **购买日期/价格** | 高 | 折旧计算、税务用途 |
| **状态** | 高 | 在库/维修中/已租出/已出售 |
| **镜头参数** | 极高 | 焦段范围、最大光圈、卡口类型、滤镜口径 — 当前完全缺失 |
| **灯光参数** | 高 | 功率(W)、色温范围(K)、闪光/常亮、引闪兼容性 |
| **兼容性** | 中 | 镜头卡口与机身匹配（如 FE 卡口 vs EF 卡口） |

**改进建议**：
```javascript
// 镜头应包含
{ n:'Sony 85mm f/1.4 GM II', c:'lens',
  brand:'Sony', mount:'FE', focalLength:'85mm', maxAperture:'f/1.4',
  filterSize:'77mm', weight:'642g', note:'...' }

// 灯光应包含
{ n:'Godox AD600 Pro', c:'light',
  power:'600W', colorTemp:'5600K', type:'flash',
  hssSupport:true, note:'...' }
```

### 3.2 场地（Venue）数据结构

**位置**：第 4482-4500 行（示例数据）、第 4562 行（渲染函数）

**当前结构**：
```javascript
{ id, name, addr, styles, contact, note, at }
```

**严重缺失的字段**：

| 缺失字段 | 重要性 | 说明 |
|----------|--------|------|
| **场地照片** | 极高 | 摄影师需要预览场地环境，当前完全缺失 |
| **场地尺寸/面积** | 高 | 决定可用焦段和布光方案 |
| **自然光条件** | 高 | 窗户朝向、采光时段、是否有天窗 |
| **电力供应** | 高 | 是否有足够电源支持影棚灯 |
| **使用费用** | 高 | 按小时/按天、是否含清洁费 |
| **预约规则** | 中 | 提前预约天数、取消政策 |
| **交通便利性** | 中 | 停车、地铁距离、搬运器材便利性 |
| **拍摄限制** | 中 | 是否允许使用闪光灯、烟雾机、是否需要报备 |
| **场地平面图** | 中 | 布光规划需要 |
| **评分/评价** | 低 | 多次使用后的评价 |

**改进建议**：
```javascript
{ id, name, addr, styles, contact, note, at,
  photos: [],           // 场地照片URL数组
  area: '80',           // 面积(平方米)
  naturalLight: '西向大窗，14:00-17:00最佳',
  power: '220V，4个插座',
  fee: '300/小时，最低2小时',
  restrictions: '禁止明火，闪光灯需提前沟通',
  rating: 4.5,          // 1-5分
  tags: ['室内', '自然光', '复古'] }
```

### 3.3 模特（Model）数据结构

**位置**：第 3089-3100 行（表单）、第 4595 行（渲染函数）、第 4612 行（提交处理）

**当前结构**：
```javascript
{ id, name, styles, contact, avail, auth, note, at }
```

**严重缺失的字段**：

| 缺失字段 | 重要性 | 说明 |
|----------|--------|------|
| **身高/体重/三围** | 极高 | 直接影响服装选择、构图角度、镜头选择 |
| **外貌照片** | 极高 | 摄影师需要预览模特外貌特征 |
| **年龄范围** | 中 | 不同年龄段妆造和光线需求不同 |
| **拍摄经验** | 中 | 新手模特需要更多引导时间 |
| **合作价格** | 中 | 商业拍摄预算核算 |
| **模特卡/作品集链接** | 中 | 评估模特表现力 |
| **尺码信息** | 高 | 服装租赁/准备需要 |
| **过敏/特殊需求** | 中 | 妆造材料、拍摄环境限制 |
| **紧急联系人** | 低 | 安全考虑 |

**当前表单的严重问题**（第 3089-3100 行）：

身高、体重、三围、脸型、肤色等关键信息被放在 `note`（备注）这个纯文本字段中（第 3099 行：`<textarea id="m-note" placeholder="身高体重、外貌特征、偏好等">`），完全没有结构化。

**改进建议**：
```javascript
{ id, name, styles, contact, avail, auth, note, at,
  height: 168,          // cm
  weight: 50,           // kg
  measurements: '84-60-88',
  age: 25,
  experience: '3年',
  photos: [],           // 模特照片
  portfolioUrl: '',
  rate: '800/小时',
  sizes: { shoe: 37, clothing: 'S' },
  allergies: '对乳胶过敏' }
```

---

## 四、工作流完整性评估

### 4.1 当前工作流覆盖度

| 阶段 | 覆盖情况 | 评分 | 说明 |
|------|----------|------|------|
| **前期策划** | 部分覆盖 | 6/10 | 有方案生成、场地库、模特库，但缺少客户沟通、报价管理、合同签署 |
| **拍摄执行** | 极少覆盖 | 2/10 | 仅有拍摄清单（第 2687-2697 行），缺少现场拍摄管理、实时标注、团队协作 |
| **后期处理** | 部分覆盖 | 4/10 | 有调色方案和 LUT 推荐，但缺少选片（culling）、精修流程、批量处理 |
| **交付管理** | 完全缺失 | 0/10 | 没有交付流程、客户审片、在线画廊、付款管理 |
| **日程管理** | 基础覆盖 | 5/10 | 有日历和飞书同步，但日程数据结构过于简单 |

### 4.2 日程管理数据结构过于简单

**位置**：第 7851-7877 行

```javascript
const newSchedule = {
    id: Date.now().toString(),
    date, title, time, location,
    createdAt: new Date().toISOString()
};
```

**缺失字段**：
- `status`（待确认/已确认/已完成/已取消）
- `clientName`（客户名称）
- `planId`（关联的拍摄方案）
- `modelId`（关联的模特）
- `venueId`（关联的场地）
- `equipmentList`（使用的器材清单）
- `notes`（备注）
- `fee`（费用）
- `duration`（拍摄时长）
- `reminder`（提醒设置）

### 4.3 完全缺失的关键工作流模块

1. **客户管理（CRM）**：没有客户信息、沟通记录、报价单、合同管理
2. **报价/财务管理**：没有报价模板、成本计算、发票管理
3. **选片流程（Culling）**：没有从大量 RAW 中筛选的功能
4. **精修管理**：没有标记待修图片、精修进度追踪
5. **在线交付画廊**：没有客户在线选片、审片、下载功能
6. **团队协作**：没有化妆师、助理等团队成员管理
7. **拍摄复盘**：没有拍摄总结、客户反馈收集

### 4.4 改进建议

**优先级 P0（核心缺失）**：
1. 增加日程与方案的关联（`planId` 字段）
2. 增加交付管理模块（在线画廊、客户审片）
3. 增加客户管理模块

**优先级 P1（重要增强）**：
1. 增加选片/精修流程管理
2. 增加报价管理
3. 增加团队成员管理

---

## 五、模板系统分析

### 5.1 模板覆盖度

**位置**：第 4626-4954 行（20 个预设模板）

| # | 模板名称 | 风格 | 场景 | 难度 | 评价 |
|---|----------|------|------|------|------|
| 1 | 日系清新人像 | 日系 | 户外 | - | 优秀，参数详细 |
| 2 | 港风复古夜景 | 港风 | 夜景 | - | 优秀，布光描述专业 |
| 3 | 复古胶片私房 | 复古 | 室内 | - | 良好，但缺少具体焦段 |
| 4 | 森系氧气写真 | 森系 | 户外 | - | 良好 |
| 5 | 商务形象照 | 商业 | 室内 | - | 优秀，色温参数精确 |
| 6 | 韩系奶油肌 | 韩系 | 室内 | - | 良好，色温4000K准确 |
| 7 | 情绪电影感 | 情绪 | 户外 | - | 良好，2.35:1宽幅专业 |
| 8 | 时尚杂志风 | 杂志 | 混合 | - | 良好，3灯布光准确 |
| 9 | 海边度假风 | 度假 | 户外 | - | 良好 |
| 10 | 法式优雅 | 法式 | 户外 | - | 良好 |
| 11 | 古风汉服 | 古风 | 户外 | - | 良好 |
| 12 | 轻婚纱写真 | 婚纱 | 户外 | 高 | 良好，新增 category/scene/difficulty 字段 |
| 13 | 文艺书店人像 | 文艺 | 室内 | 低 | 一般，内容较简略 |
| 14 | 运动活力风 | 运动 | 户外 | 中 | 一般，缺少具体布光方案 |
| 15 | 甜品店少女 | 甜美 | 室内 | 低 | 一般，过于简单 |
| 16 | 夜景霓虹情绪 | 夜景 | 夜景 | 高 | 与模板2（港风）内容高度重叠 |
| 17 | 画室艺术感 | 艺术 | 室内 | 中 | 一般 |
| 18 | 机车酷飒风 | 机车 | 户外 | 中 | 一般 |
| 19 | 樱花季限定 | 樱花 | 户外 | 中 | 一般，季节限定模板是好思路 |
| 20 | 居家慵懒风 | 居家 | 室内 | 低 | 一般 |

### 5.2 模板结构不一致问题

**严重问题**：前 11 个模板（preset-1 到 preset-11）缺少 `category`、`scene`、`difficulty` 字段，从 preset-12 开始才引入这些字段（第 4796-4798 行）。

```javascript
// preset-12 开始有这些字段
{
    id: 'preset-12',
    name: '...',
    category: ['婚纱', '情侣', '浪漫'],
    scene: '户外',
    difficulty: '高',
    // ...
}
```

这导致筛选功能（第 4984-4993 行）对前 11 个模板无效。

### 5.3 模板专业参数缺失

**所有模板共同缺失的参数**：

| 缺失参数 | 说明 |
|----------|------|
| **具体镜头推荐** | 只有 preset-3 到 preset-11 在 `guide.equipment` 中提到镜头，但格式不统一 |
| **相机参数设置** | 没有白平衡模式、对焦区域、驱动模式等 |
| **曝光补偿建议** | 没有针对不同场景的 EV 补偿建议 |
| **文件格式设置** | 没有 RAW+JPEG / 纯 RAW 建议 |
| **安全快门速度** | 没有根据焦段计算最低安全快门 |
| **布光示意图** | 没有灯光位置图（俯视图） |

### 5.4 缺失的常见摄影场景模板

| 缺失场景 | 市场需求 |
|----------|----------|
| **证件照/形象照** | 极高（日常刚需） |
| **电商产品摄影** | 高（商业需求） |
| **美食摄影** | 高（餐饮/自媒体） |
| **建筑/空间摄影** | 中（室内设计/房产） |
| **宠物摄影** | 中（增长迅速） |
| **孕妈/新生儿** | 中（高客单价） |
| **毕业照/团体照** | 中（季节性刚需） |
| **汽车/机车摄影** | 低（专业细分） |

---

## 六、计划内容渲染分析

### 6.1 `parseAIResponse` 函数

**位置**：第 5583-5646 行

**解析逻辑**：
```javascript
const sectionMap = {
    '📍 地点推荐': '📍 地点推荐',
    '🧍 摆姿指导': '🧍 摆姿指导',
    '💄 妆造建议': '💄 妆造建议',
    '🎬 道具清单': '🎬 道具清单',
    '🌤️ 天气建议': '🌤️ 天气建议',
    '🔭 焦段推荐': '🔭 焦段推荐',
    '⏰ 时间安排': '⏰ 时间安排',
    '🎨 后期调色': '🎨 后期调色',
    '⚠️ 注意事项': '⚠️ 注意事项'
};
```

**问题**：

1. **章节标题硬编码**：AI 生成的章节标题必须与 `sectionMap` 完全匹配才能正确解析。如果 AI 返回 `🔭 器材配置`（AI 提示词第 5369 行要求的是"器材配置"），但 `sectionMap` 中没有这个 key，则该章节会被丢失。
2. **Emoji 匹配正则有潜在问题**（第 5607 行）：`trimmed.startsWith(key.replace(/[\u{1F300}-\u{1F9FF}]/gu, ''))` — 这个 Unicode 范围不完整，很多摄影相关 emoji 不在此范围内。
3. **内容行匹配过于宽松**（第 5622 行）：`trimmed.length > 5` 会导致任何长度超过 5 字符的非列表文本都被当作内容，可能引入噪声。
4. **降级逻辑问题**（第 5641-5643 行）：如果解析出的 sections 少于 5 个，就回退到 `baseSections`，但 `baseSections` 的器材推荐章节内容为空（第 5207 行 `c:[]`）。

### 6.2 `renderPlanContent` 函数

**位置**：第 6084-6191 行

**渲染内容包含**：
1. 方案元数据（主题/风格/时长/人数）
2. 各章节内容（地点/摆姿/妆造/道具/天气/焦段/时间/后期/注意事项）
3. 模特沟通话术（`getCommunicationTips`）
4. 调色方案（`getColorGradingPlan`）— 包含 LUT、Lightroom 参数、HSL、曲线
5. AI 智能推荐（`getAIRecommendations`）— 器材、场地、时间、参考摄影师
6. 图片画廊

**调色方案质量评估**（第 6349 行 `getColorGradingPlan`）：

这是整个应用中**最专业的部分**。以"时尚"风格为例：
- LUT 推荐：Kodak 2383、Fuji 3510、VisionColor M31 — 均为业界知名 LUT
- Lightroom 参数精确到具体数值（色温 4800K、对比度 +20 等）
- HSL 调整覆盖了橙/黄/绿/蓝/青五个关键通道
- 曲线描述专业（S型曲线、红通道高光微抬等）
- 包含 B 站教程链接

**问题**：
- `getCommunicationTips` 和 `getColorGradingPlan` 只覆盖了部分风格（时尚、复古、文艺、性感、街拍、古风、婚纱、商业、情绪），缺少日系、韩系、港风、森系等高频风格的映射
- 调色方案中的教程链接指向 B 站，可能存在链接失效风险

### 6.3 AI 提示词与解析的不匹配（严重 Bug）

**AI 提示词**（第 5344-5387 行）要求输出 10 个章节：
```
📍 地点推荐、🧍 摆姿指导、💄 妆造建议、🎬 道具清单、🌤️ 天气与光线、🔭 器材配置、⏰ 时间流程、🎨 后期方向、⚠️ 风险预案、💡 加分技巧
```

但 `parseAIResponse` 的 `sectionMap`（第 5584-5594 行）只定义了 9 个章节，且名称不完全匹配：

| AI 提示词要求的标题 | 解析器期望的标题 | 是否匹配 |
|---|---|---|
| 🌤️ 天气与光线 | 🌤️ 天气建议 | ❌ 不匹配 |
| 🔭 器材配置 | 🔭 焦段推荐 | ❌ 不匹配 |
| ⏰ 时间流程 | ⏰ 时间安排 | ❌ 不匹配 |
| 🎨 后期方向 | 🎨 后期调色 | ❌ 不匹配 |
| ⚠️ 风险预案 | ⚠️ 注意事项 | ❌ 不匹配 |
| 💡 加分技巧 | （不存在） | ❌ 完全缺失 |

**这是一个严重的 Bug**：AI 生成的方案中，大部分章节会因为标题不匹配而被丢弃。

---

## 七、实际可用性评估（摄影师视角）

### 7.1 核心痛点

| 痛点 | 严重程度 | 说明 |
|------|----------|------|
| **8327 行单文件** | 极高 | 维护、调试、性能都是噩梦。首次加载需要解析 451KB 的 HTML |
| **数据全部存 localStorage** | 高 | 清除浏览器数据 = 丢失所有数据。虽然有备份功能（第 7442 行），但普通用户不会主动备份 |
| **日程用 prompt() 输入** | 高 | 第 7852-7857 行使用 `prompt()` 弹窗输入日程信息，体验极差，且无法输入富文本 |
| **场地库只有南京** | 高 | 示例场地全部是南京的（第 4482-4500 行），对其他城市摄影师无用 |
| **模特库为空** | 中 | `sampleModels = []`（第 4502 行），没有示例数据 |
| **没有离线支持** | 中 | 拍摄现场可能没有网络，但 AI 生成依赖在线 API |
| **MiniMax 生成限制** | 中 | 只有特定邮箱用户才能使用 MiniMax（第 5669 行硬编码邮箱判断） |
| **移动端体验差** | 中 | 虽然有响应式设计，但 451KB 的页面在手机上加载缓慢 |

### 7.2 用户体验问题

1. **方案生成后无法编辑**：生成的方案只能查看，不能修改具体章节内容
2. **模板与方案脱节**：使用模板后需要手动填写主题再生成，不能直接使用模板的完整方案
3. **场地引用功能简陋**（第 4584 行）：`useVenue` 只是把场地名称追加到场景文本框，没有关联场地 ID
4. **模特引用同样简陋**（第 4611 行）：只追加名称，不带入模特的身体特征数据
5. **没有版本控制**：方案修改后无法回退
6. **搜索功能缺失**：没有全局搜索方案/日程/模特的功能

### 7.3 亮点

1. **调色方案模块**（第 6349 行）是全应用最专业的部分，Lightroom 参数精确，LUT 推荐到位
2. **模特特征组合器**（第 2448-2525 行）的 UI 设计不错，下拉选择 + 自动组合文本
3. **专业工具链接**（第 2660-2677 行）集成了 PhotoPills、Windy、Google Earth 等实用工具
4. **拍摄清单**（第 2687-2697 行）覆盖了关键检查项
5. **多主题支持**（暗色/亮色/日出/日落）的视觉设计用心

---

## 八、综合评分与优先改进建议

### 8.1 综合评分

| 维度 | 评分(10分制) | 说明 |
|------|-------------|------|
| 摄影术语准确性 | 7.0 | 大部分准确，但缺少测光/对焦/文件格式等关键参数 |
| AI 提示词质量 | 4.0 | 缺少镜头/光圈/布光等核心摄影参数，提示词过于笼统 |
| 数据结构专业性 | 3.5 | 器材/场地/模特均缺少摄影师实际需要的核心字段 |
| 工作流完整性 | 3.0 | 仅覆盖前期策划，拍摄执行和后期交付基本空白 |
| 模板系统 | 6.5 | 覆盖面广但深度不够，结构不一致 |
| 方案渲染 | 6.0 | 调色方案优秀，但 AI 解析存在严重 Bug |
| 实际可用性 | 4.0 | 单文件架构、localStorage 存储、prompt 输入等严重影响使用 |
| **综合** | **4.9** | |

### 8.2 优先改进建议（按优先级排序）

**P0 — 必须立即修复**：

1. **修复 AI 章节解析 Bug**：统一 `genPlan` 提示词（第 5344-5387 行）中的章节标题与 `parseAIResponse`（第 5584-5594 行）中的 `sectionMap`，确保完全匹配
2. **为 AI 图片提示词添加摄影参数**：在 `buildAdvancedImagePrompt` 中加入镜头焦段、光圈值、布光方案
3. **补全模板的 category/scene/difficulty 字段**：前 11 个模板缺少筛选字段

**P1 — 短期改进**：

4. **结构化模特数据**：将身高/体重/三围等从 `note` 文本字段拆分为独立字段
5. **增强日程数据结构**：添加 status、planId、modelId、venueId 等关联字段
6. **改进日程输入 UI**：用表单替代 `prompt()` 弹窗
7. **添加数据导出/同步机制**：超越 localStorage，支持云端同步

**P2 — 中期增强**：

8. **增加后期工作流**：选片管理、精修进度追踪、交付画廊
9. **增加客户管理**：CRM、报价、合同
10. **拆分单文件架构**：至少拆分为独立的 CSS/JS 模块
