# PhotoAtelier 客户视角审查报告

**审查日期**: 2026-05-23
**审查方式**: 线上产品体验 + 前端代码审查
**审查视角**: 终端用户/客户（非开发者）

---

## 审查结论

> 作为一名摄影师客户，我打开网站后**登录就遇到了页面崩溃**。即使假设登录成功，核心功能中也存在**方案生成不实时更新、图片生成极慢（串行45-90秒）、日程管理使用浏览器原生弹窗**等严重影响体验的问题。PDF 导出的中文全部乱码。整体体验距离可用产品还有明显差距。

---

## 一、阻塞级问题（客户完全无法使用）

### 1.1 登录后页面崩溃

**客户体验**: 输入邮箱密码点击登录后，页面没有任何反应，卡在登录界面。

**实际原因**: 代码第 7164 行 `updateBadge()` 函数尝试更新 5 个导航徽章，但 HTML 中只存在 2 个（`t-tpl` 和 `t-msg`），缺少 `t-eq`、`t-venue`、`t-model`、`t-hist`。每次生成方案、加载方案等操作都会触发此崩溃。

**影响**: 这是**线上正在发生的 bug**，所有新用户首次登录后都会遇到。

---

## 二、严重问题（核心功能体验极差）

### 2.1 拍摄方案没有实时更新

**客户体验**: 点击"生成方案"后，等待 10-30 秒只能看 loading 动画，方案内容是 AI 全部生成完后一次性显示的。

**代码证据**: `genPlan` 函数使用 `fetch` + `await response.text()` 一次性获取全部内容（第 5397-5446 行），没有使用流式输出。

**客户期望**: 像 ChatGPT 一样看到方案内容逐段生成，而不是干等。

### 2.2 9 张参考图串行生成，等待 45-90 秒

**客户体验**: 点击生成图片后，一张一张慢慢出来，每张等 5-10 秒，总共要等将近 1 分半钟。

**代码证据**: 第 5716 行使用 `for` 循环串行生成，没有并行优化。

**客户期望**: 9 张图应该同时生成，或者至少分 3 组并行，10 秒内全部完成。

### 2.3 添加日程要弹 4 个浏览器原生对话框

**客户体验**: 点击"添加日程"后，连续弹出 4 个浏览器原始的输入框（日期、主题、时间、地点），每个都要点确认。手机上体验更差，键盘反复弹出收起。

**代码证据**: 第 7851-7877 行，连续 4 个 `prompt()` 调用。

**客户期望**: 应该弹出一个漂亮的表单弹窗，有日期选择器，一次性填完所有信息。

### 2.4 PDF 导出中文全部乱码

**客户体验**: 辛辛苦苦生成的方案，导出 PDF 后打开一看，所有中文都是空白或乱码。

**代码证据**: jsPDF 默认不支持中文字体（第 7177-7250 行）。

**客户期望**: 导出的 PDF 应该和页面上看到的一样，排版美观，中文正常显示。

### 2.5 图片生成失败时没有任何提示

**客户体验**: 9 张图片生成完了，只看到 6 张，另外 3 张去哪了？没有任何提示说哪些失败了。

**代码证据**: 第 5739-5741 行，catch 块是空的，失败被静默跳过。

---

## 三、中等问题（体验不流畅）

### 3.1 删除日程/方案后无法撤销

**客户体验**: 不小心删了一个日程，想撤回来发现不行。虽然代码里实现了 Undo 系统，但删除按钮没有调用它。

**代码证据**: `window.deleteSchedule`（第 7880 行）使用 `confirm()` 直接删除，没有调用已有的 `deletePlanWithUndo`。

### 3.2 查看日程详情用 alert() 弹窗

**客户体验**: 点击日历上的某天，弹出一个最原始的浏览器警告框显示日程列表。

**代码证据**: 第 7917 行使用 `alert()`。

### 3.3 没有忘记密码功能

**客户体验**: 密码忘了怎么办？页面上没有任何找回密码的入口。

### 3.4 密码不能显示/隐藏

**客户体验**: 输入密码时看不到输入了什么，尤其在手机上很容易输错。

### 3.5 Toast 通知太简陋

**客户体验**: 操作反馈只是一个底部小字，3 秒就消失了。多个通知同时出现时后者覆盖前者，完全看不到前面的。

### 3.6 模板预览图对未登录用户无法显示

**客户体验**: 还没登录就想看看模板长什么样，结果预览图加载失败。

### 3.7 PDF 导出内容不完整

**客户体验**: 导出的 PDF 只有文字方案，不包含参考图片、模特沟通话术、调色方案等附加内容。

### 3.8 移动端适配不完善

**客户体验**: 在手机上使用时，日历格子太小不好点击，部分表单元素过窄。

---

## 四、轻微问题（细节体验）

| 问题 | 客户感受 |
|------|----------|
| 自动注册无确认提示 | 不知道自己已经注册成功了 |
| 登录失败可能显示英文错误 | 看到 "Invalid credentials" 不知道什么意思 |
| 图片加载无骨架屏 | 图片加载时是空白区域，不知道要加载什么 |
| 生成按钮禁用无 spinner | 按钮变灰但没动画，不确定是不是点了 |
| 表单验证无内联提示 | 只在底部显示错误 toast，不知道哪个字段有问题 |
| 日程编辑功能未完成 | 右键菜单点"编辑"显示"开发中" |

---

## 五、做得好的地方

| 功能 | 评价 |
|------|------|
| 登录加载状态 | 有 loading spinner 和"正在登录..."提示 |
| 方案生成防重复提交 | 按钮在生成期间被禁用 |
| AI 生成失败降级 | 失败后自动使用模板生成 fallback 方案 |
| Word 导出 | 使用 HTML 格式，支持中文，内容相对完整 |
| 语言切换 | 支持中英文切换 |
| 登录表单设计 | 视觉美观，暖色调符合摄影主题 |
| 日历基本功能 | 有今日高亮、事件标记点、月份切换 |

---

## 六、客户最想看到的改进（按优先级）

### 第一优先：让产品能用起来

| # | 改进项 | 客户原话 |
|---|--------|----------|
| 1 | **修复登录崩溃** | "我登不进去啊" |
| 2 | **图片并行生成** | "等图片等太久了，能不能快点" |
| 3 | **替换原生弹窗** | "添加日程弹一堆框太烦了" |
| 4 | **修复 PDF 中文** | "导出的 PDF 中文全是乱码" |

### 第二优先：让体验流畅起来

| # | 改进项 | 客户原话 |
|---|--------|----------|
| 5 | **方案实时生成** | "能不能像 ChatGPT 一样一段一段出来" |
| 6 | **删除可撤销** | "不小心删了能不能撤回来" |
| 7 | **图片失败提示** | "有几张图没出来，为什么？" |
| 8 | **忘记密码** | "密码忘了怎么找回？" |

### 第三优先：让细节更完善

| # | 改进项 |
|---|--------|
| 9 | 密码显示/隐藏切换 |
| 10 | 移动端触控优化 |
| 11 | Toast 通知升级 |
| 12 | 图片加载骨架屏 |

---

*报告生成时间: 2026-05-23 | 审查方法: 线上产品实测 + 源码审查*
